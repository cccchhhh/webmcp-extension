# 隐私说明 — WebMCP 工具助手

更新日期：2026 年 9 月 18 日

本说明适用于本仓库的 Chrome 扩展「WebMCP 工具助手」，介绍扩展访问的数据、本地存储方式，以及 AIChat 和远程桥接功能涉及的数据传输。

## 数据访问与用途

扩展用于发现网页注册的 WebMCP 工具、展示工具定义、执行调用，以及显示调用结果。为此，扩展会处理页面标题、网址、工具名称、描述、参数结构、调用参数、执行状态和结果。参数或结果可能包含个人信息，具体取决于你使用的网页工具和输入内容。

扩展不内置广告、分析或跟踪 SDK，不主动将使用情况或浏览记录上传给开发者。扩展会读取标签页和导航信息以识别当前页面、更新工具列表和处理页面切换，不使用 Chrome 浏览历史 API 收集完整浏览历史。

手动调用由对应网页执行，网页自身可能发起网络请求或修改业务数据，其数据处理规则由该网站决定。

## 浏览器权限

| 权限 | 用途 |
| --- | --- |
| `sidePanel` | 在浏览器侧栏展示工具、聊天、设置和帮助。 |
| `activeTab`、`tabs` | 识别标签页并读取页面标题、网址及状态。 |
| `scripting` | 在获得站点访问权限后注入脚本，发现和执行 WebMCP 工具。 |
| `storage` | 保存偏好、连接配置、凭据和浏览器会话数据。 |
| `webNavigation` | 感知页面导航，更新工具状态并处理旧页面上的调用。 |
| `alarms` | 支持后台任务调度和超时处理。 |
| `offscreen` | 为后台参数校验提供离屏运行环境。 |
| 可选 HTTP/HTTPS 站点权限 | 按需授权访问网页工具、配置的模型接口或桥接地址。声明可选范围不代表默认获得所有站点访问权。 |

## 本地存储与保留

- `chrome.storage.local`：保存语言偏好、设备名称、桥接设备标识、桥接地址与插件令牌、模型配置，以及你主动选择记住的模型 API Key。这些内容可跨浏览器重启保留。
- `chrome.storage.session`：保存页面和调用状态、聊天消息、工具执行记录，以及默认仅在会话中保存的模型 API Key。此类数据不跨浏览器重启保留，扩展重载后也会清空。
- 工具调用记录最多保留 100 条，并受约 6 MiB 的预算限制；较早完成的调用可能只保留元数据。聊天记录也有容量限制，较早内容可能被裁剪。

本地存储属于浏览器扩展存储，不是操作系统密钥保险箱。扩展限制凭据存储区域仅供受信任的扩展上下文访问，但本机或浏览器配置文件的安全仍影响这些数据的安全。

## AIChat 与模型服务

AIChat 需要你配置模型接口并提供 API Key。使用时，扩展直接向配置的接口发送认证凭据、对话消息、已授权的工具定义，以及执行过程中需要的工具参数和结果。模型可能依据这些内容生成后续工具调用。

AIChat 默认授权当前兼容工具，并自动执行已授权工具；你可以调整授权范围、切换逐次确认或撤销授权。开始一轮任务后，后续模型请求和工具调用可能自动继续。停止任务可阻止后续步骤，但无法撤销已发出的请求或已执行的页面操作。

模型请求不通过本扩展开发者提供的固定中转接口。你配置的模型服务或网关会接收到上述数据，其日志、保留、训练用途和删除规则由该服务的隐私政策与账号设置决定；扩展无法替你删除服务端已经收到的数据。

## 远程桥接与外部 MCP 客户端

配置并启用桥接后，扩展会向你指定的本机或远程桥接服务发送认证信息、设备标识及共享工具目录，并转发获授权的外部客户端调用和结果。共享目录可能包含页面标题、网址的来源与路径、工具名称、描述及参数结构；目录中的页面网址会去除查询参数和片段，但工具参数、描述或结果仍可能含有敏感信息。

远程共享默认自动共享可执行工具，你可以关闭共享或取消单项工具授权。远程共享与 AIChat 使用独立授权，启用其中一项不等于启用另一项。

桥接服务运营者和获授权的外部 MCP 客户端可能访问或保留共享数据；其具体处理方式取决于你的部署、客户端配置和相关服务政策。仅连接你信任的服务，并妥善保管插件令牌和客户端凭据。

## 管理、撤销与删除

- 在浏览器扩展设置中撤销站点权限，可阻止扩展继续访问相应站点。
- 在 AIChat 中调整或撤销工具授权；在模型设置中删除配置或清除全部模型密钥。
- 在桥接设置中断开连接、清除配置，或取消页面与工具共享。已经泄露的令牌还应在对应桥接服务中撤销或轮换。
- 浏览器重启或扩展重载会清空会话存储；卸载扩展会移除该扩展的浏览器存储数据。
- 以上操作不会删除网页、模型供应商、桥接服务或外部客户端已经接收的数据；请联系相应服务方处理。

## 外部链接

Help 中的隐私说明链接会在新标签页打开本仓库的 GitHub 文档。访问 GitHub 时适用 GitHub 自身的隐私政策。本仓库为私有仓库时，查看文档需要相应的 GitHub 仓库访问权限。

## 更新与联系

数据处理方式发生变化时，本文件会更新并调整顶部日期。如有隐私问题，可在 [项目仓库](https://github.com/cccchhhh/webmcp-extension) 提出 Issue；私有仓库需要访问权限。请勿在反馈中附带 API Key、令牌或未脱敏的调用结果。
